# Secrets — never commit. Holds OPENAI_API_KEY for the local contract audit.
.env

# Audit output. Regenerated by scribe_audit.py; hand it over as files, not commits.
scribe_audit_out*/

# Local noise
.DS_Store
.playwright-mcp/
