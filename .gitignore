# Secrets — never commit. Holds OPENAI_API_KEY for the local contract audit.
.env

# Audit RESULTS are committed on purpose — they are evidence in the paper and must
# be reproducible by others. The archived model-selection runs live in
# model_selection_tests/, and the held-out verification run in
# scribe_audit_v3_heldout_out/; both are tracked, not ignored. Only the key (.env)
# and machine noise below stay out.

__pycache__/

# Local noise
.DS_Store
.playwright-mcp/
